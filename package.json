{
  "name": "geogirafe",
  "description": "Simple Geo Viewer",
  "author": "Guillaume REMY",
  "version": "0.1",
  "devDependencies": {
    "vite": "^3.1.4"
  },
  "dependencies": {
    "ol": "^7.1.0"
  },
  "scripts": {
    "serve": "cd src && cp --parents -R components/**/*.html static/ && cd .. && vite serve",
    "build": "cd src && cp --parents -R components/**/*.html static/ && cd .. && vite build"
  }
}
